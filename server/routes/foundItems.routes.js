import express from "express";

const FoundItemRouter = express.Router();

FoundItemRouter.get("/", async (req, res) => {});
FoundItemRouter.post("/", async (req, res) => {});
FoundItemRouter.get("/:userid/:id", async (req, res) =>{});

export default FoundItemRouter;
